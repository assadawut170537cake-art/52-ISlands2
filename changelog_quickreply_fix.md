# 📝 Changelog: แก้ไขบั๊ก Quick Reply (ทำที่เดิม/งานเดิม)

**วันที่:** 1 กรกฎาคม 2026
**ผู้ดำเนินการ:** J.A.R.V.I.S. (AI Assistant)

## 📌 สาเหตุของปัญหา
ปุ่ม Quick Reply (เช่น "ทำที่เดิม/งานเดิม", "เปลี่ยนไซต์/เปลี่ยนงาน", "ยกเลิกลงเวลา") เมื่อกดแล้ว ระบบ LINE จะส่งข้อมูลแบบ `postback` กลับมาพร้อมเครื่องหมาย `#` นำหน้า (เช่น `#ทำที่เดิม/งานเดิม`) 

แต่ในโค้ดเดิมไฟล์ `1_bot line.js` มีบั๊ก 2 จุดคือ:
1. โค้ดดักจับ `#` เพื่อเคลียร์สถานะค้าง (Clear State) ดันไปทำงานกับ `postback` ด้วย ทำให้เมื่อกดปุ่ม State ที่ค้างอยู่ (เช่น การรอคำตอบทำโอที) ถูกล้างออกหมด
2. โค้ดที่ประมวลผล State ดันใช้ตัวแปร `msg` (ซึ่งมี `#` ติดอยู่) ไปตรวจสอบเงื่อนไขแบบเป๊ะๆ `if (msg === "ทำที่เดิม/งานเดิม")` ซึ่งไม่มีทางเป็นจริง

## 🛠️ การแก้ไขที่ทำไป
**ไฟล์ที่แก้ไข:** `1_bot line.js`
1. **เพิ่ม Guard ไม่ให้ Postback เคลียร์ State:**
   - แก้ไขบรรทัด: `if (!isPostback && msg.startsWith("#") && (pendingClockIn12 || pendingOTConfirm || pendingOTDetails))`
   - **ผลลัพธ์:** ทำให้ผู้ใช้ยังพิมพ์ `#` เพื่อเคลียร์ State ได้ตามเดิม แต่ปุ่ม Quick Reply จะไม่โดนเคลียร์
2. **เปลี่ยนตัวแปรที่ใช้เปรียบเทียบคำ:**
   - แก้ไขการเช็ค `msg === ...` ให้กลายเป็น `cmdTrimmed === ...` ในบล็อกของ `pendingOTDetails`, `pendingOTConfirm` และ `pendingClockIn12`
   - **ผลลัพธ์:** ทำให้ตัวแปรที่เปรียบเทียบ ไม่มีเครื่องหมาย `#` นำหน้า และเข้าเงื่อนไขได้อย่างถูกต้อง

## 🚀 ผลการทดสอบ
- ระบบสามารถแยกแยะระหว่างการพิมพ์ `#` ปกติ กับการกดปุ่ม Quick Reply ได้แล้ว
- เมื่ออยู่ในโหมดรอการยืนยันทำโอที และกดปุ่ม "ทำที่เดิม/งานเดิม" ระบบจะบันทึกสถานะได้ถูกต้องตามต้องการ

## 🔄 ระบบ CI/CD
- โค้ดถูก Deploy ขึ้น Google Apps Script ผ่าน `clasp push` เรียบร้อยแล้ว
- ทำการ Commit โค้ดกลับไปยังระบบ Git (Local)
