# สรุปรายการแก้ไขโปรเจกต์ (Changelog)
**วันที่:** 2026-07-03
**ชื่องาน:** ปรับปรุงระบบ Webhook และการลงเวลา (OT & Time Calculation)

## รายการแก้ไขทั้งหมด:

### 1. `1_bot line.js` (ศูนย์กลางการรับส่งข้อมูล Webhook)
- **เพิ่มระบบ LockService (Race Condition Prevention):** ใน `doPost(e)` เพื่อป้องกันปัญหาพนักงานกดปุ่มหรือส่งข้อความเข้ามาพร้อมกันจนข้อมูลชนกัน (กำหนด timeout ที่ 10 วินาที)
- **ปรับปรุง `handleLineWebhook`:**
  - สร้างระบบ Normalization ดึงค่าเก็บในตัวแปรกลาง `actionData` ไม่ว่าคำสั่งจะมาจาก Text Message หรือ Postback Data (การกดปุ่ม)
  - นำ `actionData` ส่งต่อเข้าไปในระบบตรวจสอบการลงโอที เช่น "ทำที่เดิม/งานเดิม" เพื่อให้ปุ่ม Quick Reply และ Flex Message ใช้งานกับระบบเก่าได้ 100%
  - เพิ่ม Fallback แบบเก่า (`let msg = actionData;`) ป้องกัน Legacy code อื่นพัง
- **เพิ่ม JSDoc:** อธิบายพารามิเตอร์ของ `doPost` และ `handleLineWebhook` ชัดเจน เพื่อความสะดวกในการดูแลรักษา

### 2. `Config.js` (การตั้งค่าระบบ)
- **ลบ Hard-coded Secrets:** ลบ Token ของ LINE, Gemini, Database ID และ Admin ID ที่ฝังในโค้ดออกทั้งหมด เพื่อบังคับให้ดึงค่าผ่านระบบความปลอดภัย `PropertiesService` แทน

### 3. `11_WageCalculation_Core.js` และ `4_CoreDatabase.js`
- **ปรับปรุงตรรกะการคำนวณเวลา (Time Parsing):** นำฟังก์ชันการแปลงเวลาที่ต้องสร้าง Date Object ซึ่งกินทรัพยากรออก และเปลี่ยนไปใช้ฟังก์ชันคณิตศาสตร์แปลงเป็นนาที (`toM`) แทน ทำให้ระบบคำนวณเร็วขึ้น ลดความซับซ้อน และลบ wrapper `executeWithLock` ออกจาก core functions เนื่องจากมีการใช้ lock ตั้งแต่ต้นทางแล้ว

### 4. การอัปเดตเอกสารโครงสร้างโปรเจกต์
- `PROJECT_STRUCTURE.md` ได้รับการอัปเดตเพื่อรองรับสถานะใหม่ของการทำงาน

## สถานะ:
ดำเนินการปรับปรุงและทดสอบความเสถียรในเชิง Logic โค้ดเสร็จสิ้นแล้ว พร้อม Deploy และนำขึ้นระบบจริง
