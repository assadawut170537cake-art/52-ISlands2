# Changelog: Restore AI Fallback and Drive ID Lookup

## วันที่
06 กรกฎาคม 2026

## สรุปงาน
ทำการแก้ไขปัญหาระบบแจ้งเตือน "ระบบขัดข้อง กรุณาแจ้ง Admin" ในขั้นตอนการส่งข้อมูลจากบอทไลน์ (LINE Bot) โดยทำการกู้คืนฟังก์ชันที่สูญหายจากการทำ Project Isolation กลับสู่ระบบหลัก

## รายการการแก้ไข
1. **กู้คืนฟังก์ชันดึง ID ของไฟล์บันทึกรายเดือน** 
   - ไฟล์: `3_SharedFunctions.js`
   - นำฟังก์ชัน `getTargetFileIdByDate(dateStr)` ที่เคยอยู่ในระบบจัดการ Google Drive กลับมาใส่ไว้ใน `3_SharedFunctions.js` เพื่อให้ `1_bot line.js` สามารถนำไปคำนวณและเข้าถึงชีตข้อมูลรายเดือนได้อย่างถูกต้อง แก้ปัญหา Error ในกระบวนการทำงานหลัก (Clock-In/OT)

2. **สร้างแกนหลักสำหรับ AI ในโปรเจกต์เดิม (AI Fallback Core)**
   - ไฟล์: `7_AI_Core.js` (สร้างใหม่)
   - นำฟังก์ชัน `callGemini` และ `callGeminiVision` กลับเข้ามาและติดตั้งแยกเป็น Standalone ในระบบ `Smart Worksite` เพื่อไม่ให้รบกวนกับโปรเจกต์ `J.A.R.V.I.S.` ที่แยกออกไปก่อนหน้านี้
   - ระบบนี้จะช่วยรองรับการประมวลผลกรณีที่มีการส่งรูปภาพบัตรตอก หรือส่งข้อความสุ่มอื่นๆ ที่ไม่ตรง Regex ของระบบ

## สถานะการแก้ไข (Status)
- รันและทดสอบทำงานได้จริง 100%
- Deploy โค้ดล่าสุดขึ้น Web App และทดสอบการทำงานของบอทแล้ว
