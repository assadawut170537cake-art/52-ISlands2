# Changelog: แก้ไขตรรกะการคำนวณ OT 8 ชั่วโมง และซ่อมแซมเมนูที่หายไป

## รายละเอียดการแก้ไข
ในรอบการทำงานนี้ ได้มีการปรับปรุงตรรกะการคำนวณล่วงเวลา (OT) ให้เป็นไปตามกฎบริษัท คือ **ต้องทำงานรวมให้ครบ 8 ชั่วโมง (480 นาที) ก่อน ถึงจะเริ่มปัดเวลาที่เหลือเป็น OT** ไม่ว่าพนักงานจะเลิกงานหลัง 17:00 น. หรือไม่ก็ตาม หากชั่วโมงรวมไม่เกิน 8 ชั่วโมง ระบบจะลงเวลาเป็นงานปกติทั้งหมด นอกจากนี้ยังได้มีการแก้ไขบั๊กเมนูที่หายไปจากหน้า Sheet เนื่องจากไฟล์ขยะที่เผลออัปโหลดขึ้นไป

## ไฟล์ที่แก้ไข
1. **`1_bot line.js`**
   - **ฟังก์ชัน:** `checkOTAndProceed`
   - **รายละเอียด:** อัปเดตตรรกะให้คำนวณเวลาทำงานสุทธิ (หักเวลาพักเที่ยง) หาก `actualWorkMins <= 480` จะปรับให้ `otMins = 0` แทนที่จะเช็คเวลาเลิกงานอย่างเดียว

2. **`4_CoreDatabase.js`**
   - **ฟังก์ชัน:** `calculateAndTimeEntryFromValues`
   - **รายละเอียด:** อัปเดตเงื่อนไขให้ตัดชั่วโมงการทำงานปกติที่ 8 ชั่วโมง หากมีเวลาเกินถึงจะถูกจัดสรรลงในคอลัมน์ OT เที่ยง, เช้า และเย็นตามลำดับ

3. **`3_SharedFunctions.js`**
   - **ฟังก์ชัน:** `calculateAndTimeEntry`
   - **รายละเอียด:** ปรับปรุงลอจิกเดียวกับ `4_CoreDatabase.js` เพื่อให้ทำงานเป็นมาตรฐานเดียวกันทั้งระบบ

4. **การลบไฟล์ขยะและซ่อมระบบเมนู (DevOps)**
   - **รายละเอียด:** นำไฟล์ทดสอบ (`_all.js`, `_gas_check.js`) ที่ทำให้เกิดปัญหาตัวแปรซ้ำซ้อน (เช่น `SHEET_WORKSPACE`) บน Google Apps Script ออก และทำการ Force Push ให้โครงสร้างกลับมาเป็นปกติ เพื่อแก้ปัญหา `onOpen()` ที่ไม่ทำงานจนเมนูหายไป
