# Changelog: ปรับปรุงฟังก์ชัน syncDirectFromGAS (Code Workspace)

## วันที่และเวลา
2026-07-02

## รายละเอียดการแก้ไข
- **ฟังก์ชันที่ได้รับผลกระทบ**: `syncDirectFromGAS()` ในไฟล์ `10_DevOps_Core.js`
- **ปัญหาเดิม**: ระบบดึงทุกบรรทัด (รวมถึงคอมเมนต์และบรรทัดว่าง) ไปแสดงผลเป็นแถวใหม่ในหน้าแผ่นงาน ทำให้ตารางรกและไม่เป็นไปตามโครงสร้างที่ต้องการ
- **การแก้ไข**: 
  - เพิ่มลอจิกการสะสมคอมเมนต์ (//, /*, *, */) เพื่อให้เป็น Auto Description
  - กรองให้สร้างบรรทัดใหม่เฉพาะเมื่อพบบรรทัดที่ตรงกับการประกาศ `function` เท่านั้น (1 บรรทัดต่อ 1 ฟังก์ชัน)
  - นำคอมเมนต์ที่สะสมไว้ไปใส่ในคอลัมน์ G (คำอธิบายฟังก์ชันอัตโนมัติ) และใช้เลขบรรทัดจริงใส่ในคอลัมน์ B
  - ยกเลิกการเรียกใช้ SetFormulas ที่ขัดแย้ง
- **สถานะ**: นำไปวางใน `10_DevOps_Core.js` และ Deploy เข้าสู่โปรเจกต์ GAS เรียบร้อยแล้ว
