# 52 Island - Isolated Workspace Architecture (5 Projects)

## 📌 ภาพรวมระบบ (System Overview)
ระบบถูกออกแบบภายใต้สถาปัตยกรรม **Isolated Workspace** เพื่อแยกบริบท (Context Isolation) ของทั้ง 5 โปรเจกต์หลักออกจากกันอย่างเด็ดขาด ป้องกันการปนเปื้อนของข้อมูลและตัวแปร แต่ละโปรเจกต์มีขอบเขตและหน้าที่รับผิดชอบชัดเจน ดังนี้:

---

## 1. [52 Island - Smart Worksite System]
ระบบจัดการฐานข้อมูลและบันทึกเวลาทำงานอัตโนมัติ ที่เชื่อมต่อผ่าน LINE Bot และ Web App 
ทำหน้าที่จัดการข้อมูลพนักงาน, ลงเวลาเข้า-ออกงาน ผ่าน Google Apps Script (GAS) โดยใช้ Google Sheets เป็นฐานข้อมูลหลัก
- **ไฟล์ 'DATA'**: ฐานข้อมูลหลัก (Master Database) ใช้สำหรับอ้างอิงรายชื่อพนักงาน ข้อมูลสิทธิ์ และการตั้งค่าต่างๆ ของระบบ (อ้างอิงการค้นหาชื่อจากหน้า 'รายชื่อพนักงาน' เป็นหลัก)
- **ไฟล์พื้นที่รายเดือน**: ใช้สำหรับบันทึกรายงานประจำวัน (Daily logs) เช่น การบันทึกเวลาเข้า-ออก, สถานที่ทำงาน, และโอที
  - *เงื่อนไขสำคัญ*: บันทึกย้อนหลัง 1 วัน (เมื่อวาน) = ปกติ / ย้อนหลังเกิน 2 วัน = ล่าช้า
- **โครงสร้างไฟล์หลัก**: `appsscript.json`, `main manu` (ศูนย์กลางโค้ด), `1_bot line.js`, `2_WebApp.js`, `4_CoreDatabase.js`

## 2. [J.A.R.V.I.S. - Local AI Assistant]
สมองกลผู้ช่วยสำหรับการประมวลผลคำสั่งผ่านการแชท (AI Logic) ตรวจสอบสิทธิ์ (Admin) อัปเดตกฎแบบแปรผัน (Dynamic Rules) และประมวลผลข้อความที่ไม่ตรงแพทเทิร์นตายตัว
- **หน้าที่หลัก**: วิเคราะห์รูปภาพ (Vision API), แยกแยะเจตนาผู้ใช้ (Intent Classification), และเชื่อมต่อระหว่างผู้ใช้กับระบบอื่นๆ
- **สถานที่จัดเก็บ**: แยกออกไปอยู่ที่โฟลเดอร์ `../JARVIS_Library`

## 3. [PayrollSystem 52 - Time & Payroll Management]
ระบบบัญชีและคำนวณเงินเดือน ประมวลผลรอบบิลค่าแรง และจัดการการขอโอที
- **หน้าที่หลัก**: ดึงข้อมูลจากการบันทึกเวลาของ Smart Worksite System มาประมวลผลและสรุปยอดค่าแรง
- **สถานที่จัดเก็บ**: แยกออกไปอยู่ที่โฟลเดอร์ `../PayrollSystem_Library`

## 4. [BOQ System - Material & Cost Estimation]
ระบบคำนวณวัสดุสิ้นเปลือง ค่าแรง ค่าวัสดุ และสร้างเอกสาร BOQ
- **เทคโนโลยี**: Google Sheets, Google Apps Script (GAS)
- **ฐานข้อมูลอ้างอิง**: สเปกอุปกรณ์วิศวกรรมไฟฟ้า (เช่น ตู้คอนโทรล 3-Phase, Impulse relays, Magnetic contactors, Inverters) และงานประปา/สุขาภิบาล
- **กฎธุรกิจ**: โฟกัสความแม่นยำในการเปรียบเทียบสเปกวัสดุและคำนวณต้นทุนหน้าไซท์งาน

## 5. [Google Drive Management System]
ระบบจัดการและจัดระเบียบข้อมูลภายใน Google Drive อัตโนมัติ
- **ฟีเจอร์หลัก**: การค้นหาไฟล์ (DriveSearchService), การจัดการสิทธิ์, การจัดเก็บไฟล์ภาพหรือเอกสารที่เชื่อมโยงกับโปรเจกต์อื่นๆ
- **กฎการทำงาน**: ต้องมีระบบแคช (Cache) หรือการค้นหาที่ปรับแต่งมาเพื่อลดเวลาประมวลผล (Timeout) เมื่อดึงไฟล์จำนวนมาก
- **สถานที่จัดเก็บ**: แยกออกไปอยู่ที่โฟลเดอร์ `../GoogleDrive_Library`

---

## 📂 โครงสร้างไฟล์ในระบบรวม (Shared / Cross-Project Utilities)
- **การตั้งค่าระบบ**: `Config.js` / `Script properties.js`
- **เครื่องมือใช้งานร่วม**: `3_SharedFunctions.js` (ฟอร์แมตวันที่, ดึงข้อมูลพนักงาน), `DailySheetProcessor.js`
- **ระบบความปลอดภัยและ CI/CD**: 
  - `0_Security.js` / `AccessControl.js` (RBAC, Whitelist)
  - `10_DevOps_Core.js` / `6_DeploymentService.js` (CI/CD, Checkpoint, Backup)
  - `3_BigQueryLogger.js` / `5_CloudLoggingService.js` (Audit Trail)
- **การประมวลผลด้วย AI (AI Fallback)**: `7_AI_Core.js` (สกัดและคืนสภาพฟังก์ชันจาก JARVIS ให้โปรเจกต์หลักทำงานได้แบบ Standalone)
- **หน้าจอ Dashboard (Frontend HTML)**: `index.html`, `SmartWorksiteDashboard.html`, `DevOps_Dashboard.html`, `System_Settings.html`, ฯลฯ
- **ประวัติการแก้ไข (Changelog)**: `changelog_code_review.md`, `changelog_project_isolation.md`, `changelog_dashboard_fix_and_isolation_alert.md`, `changelog_restore_ai_fallback.md`, `changelog_update_agents_rules.md`, `changelog_update_20260712.md`, `changelog_auto_timecard_and_line_notifications.md`, `changelog_undo_last_action_group_fix.md`, `changelog_line_bot_fixes_and_pdf_export_20260806.md`
- **การปรับปรุงล่าสุด (August 2026)**:
  - แก้ไขระบบ `#ยกเลิกล่าสุด` ใน LINE Group โดยส่ง `groupId` ลงไปเก็บ Cache ทั้ง 2 คีย์ (`LAST_ENTRY_${userId}` และ `LAST_ENTRY_${groupId}`)
  - แก้ไขลอจิก `OTต่อเนื่อง` ป้องกันการบังคับเริ่ม OT เย็นที่ 17:30 น. เพื่อคำนวณชั่วโมง OT ได้เต็มจำนวน 100%
  - แก้ไขโหมด `แก้ไข` ให้ทำการเคลียร์รายการเดิมของพนักงานตามวันที่ที่ระบุก่อนลงบันทึกข้อมูลใหม่แทนที่
  - แก้ไขการแปลงปี พ.ศ. 2 หลัก (เช่น `69` -> 2569 BE -> 2026 AD) เพื่อความแม่นยำในการเปรียบเทียบวันที่
- **โมดูลประมวลผลพิเศษ**: `13_AutoTimecardProcessor.js` (ประมวลผลบัตรตอกและถอดความเสียงภาษาธรรมชาติ ส่งรายงานเข้ากลุ่มอัตโนมัติ)

---

*Generated by AI Assistant - Last Updated: 2026-08-06 (Deployment Version @790)*
