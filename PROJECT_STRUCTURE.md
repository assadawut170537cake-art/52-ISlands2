# Smart Worksite System - Project Structure

## 📌 ภาพรวมโปรเจกต์ (Project Overview)
Smart Worksite System เป็นระบบจัดการฐานข้อมูลและบันทึกเวลาทำงานอัตโนมัติ ที่เชื่อมต่อผ่าน LINE Bot และ Web App 
ทำหน้าที่จัดการข้อมูลพนักงาน, ลงเวลาเข้า-ออกงาน, ประมวลผลเงินเดือน, และจัดการการขอโอที ผ่าน Google Apps Script (GAS) 
โดยใช้ Google Sheets เป็นฐานข้อมูลหลัก

## 🗄️ ระบบฐานข้อมูลหลัก (Core Databases)
1. **ไฟล์ 'DATA'**: ฐานข้อมูลหลัก (Master Database) ใช้สำหรับอ้างอิงรายชื่อพนักงาน ข้อมูลสิทธิ์ และการตั้งค่าต่างๆ ของระบบ 
   - อ้างอิงการค้นหาชื่อจากหน้า 'รายชื่อพนักงาน' เป็นหลัก
2. **ไฟล์พื้นที่รายเดือน (เช่น 'มิถุนายน')**: ใช้สำหรับบันทึกรายงานประจำวัน (Daily logs) เช่น การบันทึกเวลาเข้า-ออก, สถานที่ทำงาน, และโอที
   - เงื่อนไขสำคัญ: บันทึกย้อนหลัง 1 วัน (เมื่อวาน) = ปกติ / ย้อนหลังเกิน 2 วัน = ล่าช้า

## 📂 โครงสร้างไฟล์ในระบบ (File Structure)

### ไฟล์ศูนย์กลางและระบบการจัดการ (Core & Management)
- **`appsscript.json`**: ไฟล์ตั้งค่าโปรเจกต์ (Manifest) ของ Google Apps Script
- **`main manu`** *(Google Sheet ภายนอก)*: ไฟล์ศูนย์กลางระบบสำหรับตรวจสอบโค้ดและชื่อฟังก์ชัน (หน้า `Code_Workspace`)
- **`Config.js`** / **`Script properties.js`**: จัดการการตั้งค่า PropertiesService, ตัวแปรระบบ, และ API Key ต่างๆ

### ระบบเชื่อมต่อผู้ใช้ (User Interfaces & Webhooks)
- **`1_bot line.js`**: จัดการ Webhook ของ LINE Bot, รับข้อความ/รูปภาพ, และตอบกลับผู้ใช้ (รวมถึงการจัดการ State ของ Quick Reply)
- **`2_WebApp.js`**: ตัวจัดการฝั่ง Web Application รับ HTTP Request (doGet/doPost) สำหรับ Dashboard
- **`9_System_Chat.js`**: ฟังก์ชันสนทนาและการตอบสนองของระบบแชท
- **`changelog_quickreply_fix.md`**: ไฟล์สรุปการแก้บั๊กปุ่ม Quick Reply และกระบวนการ CI/CD (วันที่ 1 ก.ค. 2026)
- **`changelog_webhook_refactor.md`**: ไฟล์สรุปการปรับปรุงระบบ Webhook (LockService, Normalization) และการคำนวณเวลา (วันที่ 3 ก.ค. 2026)


### สมองกลผู้ช่วย (AI Assistant)
- **`7 _AI_Assistant.js`**: ประมวลผลคำสั่งผ่านการแชท (AI Logic), ตรวจสอบสิทธิ์ (Admin), อัปเดตกฎแบบแปรผัน (Dynamic Rules), และประมวลผลข้อความที่ไม่ตรงแพทเทิร์นตายตัว
- **`2_VisionAPI.js` / `VisionInterface.js`**: ระบบวิเคราะห์และประมวลผลรูปภาพบัตรตอกหรือหน้าบุคคล

### ระบบฐานข้อมูลและการจัดการไฟล์ (Database & Storage)
- **`4_CoreDatabase.js`**: ฟังก์ชันหลักที่เชื่อมต่อและจัดการข้อมูลใน Google Sheets (DATA และ พื้นที่รายเดือน)
- **`3_SharedFunctions.js`**: ฟังก์ชันสนับสนุนต่างๆ ที่ใช้ร่วมกันในระบบ (เช่น การฟอร์แมตวันที่, การดึงข้อมูลพนักงาน)
- **`7_CloudStorageService.js` / `4_DriveSearchService.js`**: จัดการการเข้าถึง, บันทึก และค้นหาไฟล์/รูปภาพ ใน Google Drive
- **`DailySheetProcessor.js`**: ฟังก์ชันสำหรับประมวลผลสรุปรายการประจำวัน (Daily sheet processing)

### ระบบบัญชีและคำนวณเงินเดือน (Payroll & Finance)
- **`11_WageCalculation_Main.js`**: ฟังก์ชันหลักในการควบคุมและรันรอบบิลค่าแรง
- **`11_WageCalculation_Core.js`**: Core Logic การคำนวณเงินเดือน
- **`11_WageCalculation_Utils.js`**: Utility ฟังก์ชันสำหรับหนุนระบบคำนวณค่าแรง

### ระบบความปลอดภัยและ CI/CD (Security & DevOps)
- **`0_Security.js` / `AccessControl.js`**: จัดการสิทธิ์การเข้าถึง (RBAC), ตรวจสอบ Whitelist, และความปลอดภัยต่างๆ
- **`10_DevOps_Core.js` / `6_DeploymentService.js`**: จัดการเรื่อง CI/CD, การสร้าง Checkpoint, Backup, และ Version Control
- **`3_BigQueryLogger.js` / `5_CloudLoggingService.js`**: ระบบ Audit Trail เพื่อเก็บ Log เหตุการณ์ลง BigQuery หรือแหล่งอื่นๆ สำหรับการตรวจสอบ
- **`changelog_syncDirectFromGAS.md`**: ไฟล์สรุปการอัปเดตฟังก์ชันดึงรหัสจาก GAS (1 บรรทัดต่อ 1 ฟังก์ชัน)
- **`changelog_bot_cache_fix.md`**: ไฟล์สรุปการแก้บั๊ก Cache และปรับประสิทธิภาพบอทไลน์ (2 ก.ค. 2026)
- **`changelog_ot_payload_and_date_validation.md`**: ไฟล์สรุปการแก้ปัญหา Quick Reply Payload และการบังคับใช้ปีปัจจุบัน (2 ก.ค. 2026)

### หน้าจอ Dashboard (Frontend HTML)
- **`index.html`**, **`SmartWorksiteDashboard.html`**, **`DevOps_Dashboard.html`**, **`Interactive_Manual.html`**, **`InjectorSidebar.html`**: ไฟล์แสดงผลสำหรับหน้า Web App และส่วนขยาย (Add-on/Sidebar)

---
*Generated by AI Assistant (Start Work & Checkpoint Skill) - Last Updated: 2026-07-03*
